---
import PageDocument from '../../components/app/PageDocument.astro';
import { ProjectsPageIsland } from '../../components/app/PageIslands';
import { getPageMetadata } from '@/data/pages';
const metadata = getPageMetadata("project2");
---
<PageDocument
  title={String(metadata.title)}
  description={String(metadata.description)}
  canonicalPath="/projects/"
>
  <ProjectsPageIsland client:load pathname={Astro.url.pathname} />
</PageDocument>