---
import PageDocument from '../../components/app/PageDocument.astro';
import { FeatureYapayZekaSistemEntegrasyonuPageIsland } from '../../components/app/PageIslands';
import { getDemoMetadata } from '@/data/demo';
const metadata = getDemoMetadata(13);
---
<PageDocument
  title={String(metadata.title)}
  description={String(metadata.description)}
  canonicalPath="/yapay-zeka-sistem-entegrasyonu/"
>
  <FeatureYapayZekaSistemEntegrasyonuPageIsland client:load pathname={Astro.url.pathname} />
</PageDocument>