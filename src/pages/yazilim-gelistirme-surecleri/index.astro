---
import PageDocument from '../../components/app/PageDocument.astro';
import { FeatureYazilimGelistirmeSurecleriPageIsland } from '../../components/app/PageIslands';
import { getDemoMetadata } from '@/data/demo';
const metadata = getDemoMetadata(1);
---
<PageDocument
  title={String(metadata.title)}
  description={String(metadata.description)}
  canonicalPath="/yazilim-gelistirme-surecleri/"
>
  <FeatureYazilimGelistirmeSurecleriPageIsland client:load pathname={Astro.url.pathname} />
</PageDocument>